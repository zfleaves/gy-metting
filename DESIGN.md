# AI 智能会议纪要轻量化中台设计方案

文档版本：V1.2
编制日期：2026-08-17
适用范围：部门内部需求评审、技术评审、迭代周会
方案定位：单机轻量化业务中台原型，可演进为企业级正式中台；模型无关，支持对接各类大模型 API；不依赖腾讯会议 / 钉钉会议企业版权限。

---

## 目录

1. [方案概述](#1-方案概述)
2. [整体架构设计](#2-整体架构设计)
3. [核心功能详细设计](#3-核心功能详细设计)
4. [完整业务处理流程](#4-完整业务处理流程)
5. [内部模块拆解](#5-内部模块拆解)
6. [质量与防幻觉设计](#6-质量与防幻觉设计)
7. [部署方案](#7-部署方案)
8. [实施周期与里程碑](#8-实施周期与里程碑)
9. [风险点与应对措施](#9-风险点与应对措施)
10. [后续演进规划](#10-后续演进规划)
11. [方案总结](#11-方案总结)

---

## 1 方案概述

### 1.1 建设目标

搭建部门级轻量化 AI 会议纪要中台，解决评审会议纪要编写耗时、决策遗漏、待办责任人不清晰、口头讨论与原始需求脱节等痛点。

**核心目标**：
- 不依赖腾讯会议、钉钉会议企业版，支持录音音频上传完成会议纪要生成；
- 集成语雀，输入文档链接自动拉取 PRD 作为需求基线，实现评审变更对照；
- 支持上传本地参考文档（docx/pdf/txt/md），与语雀文档共同构成需求基线，覆盖无语雀权限场景；
- 支持业务背景录入、支持自定义系统提示词，可灵活适配不同大模型；
- 输出可读 Markdown 用于群聊推送，同时输出机器可解析结构化 JSON；
- 支持钉钉机器人消息推送；
- 全量会议元数据、PRD 快照、上传文档快照、原始转写、AI 输出结果本地归档，可回溯审计；
- 中台化设计，不是单点脚本，支持后续对接内部其他业务系统。

### 1.2 适用业务场景

- 产品需求评审会（核心场景，PRD 对照）
- 技术方案评审会
- 项目迭代周会、同步例会
- 各类需要对照参考文档开展的内部评审会议

### 1.3 建设原则

- **数据内网闭环**：录音、原始转写文本不出内网，保障业务信息安全；语雀文档拉取、钉钉推送等需出公网的操作，只传输非敏感的业务数据；
- **模型无关**：业务逻辑与大模型解耦，通过统一 LLM 适配器接口对接 DeepSeek、通义千问、GLM 等公有或私有化大模型；
- **最小可用优先**：先实现核心闭环（API + CLI），再叠加增强能力（Web 界面）；
- **可回溯可审计**：每一次 AI 生成，保存当时的背景、提示词、参考文档快照，便于事后核对；
- **向后兼容扩展**：架构预留接口，未来可对接会议开放平台、OA、项目管理工具。

### 1.4 数据安全分级

| 安全等级 | 数据类型 | 处理方式 |
|----------|---------|---------|
| **L1 敏感** | 音频文件、原始 ASR 转写文本 | 严格不出内网；仅存储于本地，不发送至任何外部 API |
| **L2 业务** | 会议纪要结果、结构化 JSON | 可发送至钉钉等外部渠道，仅传输摘要级内容 |
| **L3 公开** | 语雀文档内容（本身已在公网） | 可正常拉取，快照本地存储 |

---

## 2 整体架构设计

### 2.1 四层分层架构

```
┌──────────────────────────────────────────────────────────────┐
│                    接入层 (Access Layer)                      │
│  ┌──────────────────────┐  ┌──────────────────────────────┐  │
│  │  CLI 命令行工具       │  │  HTTP 标准化 API             │  │
│  │  (开发/初期使用)      │  │  (程序调用/系统对接)          │  │
│  └──────────────────────┘  └──────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│                  任务调度层 (Task Scheduler)                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  异步任务队列（内存队列 + 数据库持久化）                │   │
│  │  任务提交 → 返回 task_id → 轮询/回调获取结果           │   │
│  └──────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────┤
│                  中台核心能力层 (Core Layer)                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────────────────┐  │
│  │ 本地离线  │ │ 参考文档   │ │     AI 纪要推理引擎          │  │
│  │ASR 模块  │ │ 适配器    │ │  (LLM 适配器 + 模板管理      │  │
│  │          │ │(语雀/本地)│ │   + 上下文组装)               │  │
│  └──────────┘ └──────────┘ └──────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              上下文预处理模块                          │   │
│  │        (超长截断、清洗、防上下文超限)                   │   │
│  └──────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────┤
│                   输出适配层 (Output Layer)                    │
│  ┌──────────────────┐ ┌──────────────┐ ┌──────────────────┐  │
│  │ 钉钉消息适配器    │ │ 结果格式化    │ │ 存储写入适配器   │  │
│  │ (Markdown 推送)  │ │ (MD + JSON)  │ │ (数据库持久化)   │  │
│  └──────────────────┘ └──────────────┘ └──────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│                      存储层 (Storage Layer)                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  SQLite (WAL 模式) → MySQL (平滑迁移)                 │   │
│  │  持久化：会议主题、业务背景、提示词、参考文档快照       │   │
│  │  (语雀/本地)、原始转写、Markdown 纪要、                  │   │
│  │  结构化 JSON、推送信息、时间                             │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 数据流总览

```
用户输入（音频 / SRT 文本、会议主题、业务背景、自定义提示词、语雀文档链接 / 本地参考文档）
       │
       ▼
任务提交 → 返回 task_id（异步处理，避免 HTTP 超时）
       │
       ▼
文件处理 & 参考文档拉取 → 生成参考文档快照（语雀 / 本地上传）
       │
       ▼
LLM 适配器封装请求（统一接口，支持多模型切换）
       │
       ▼
上下文组装：系统模板 + 业务背景 + 参考文档快照 + 会议转写文本
       │
       ▼
请求大模型，输出 Markdown + JSON 结构化纪要
       │
       ├──→ 可选钉钉推送通知（需公网访问）
       │
       ▼
全量数据入库持久化
       │
       ▼
用户通过 task_id 轮询获取结果
```

---

## 3 核心功能详细设计

### 3.1 会议原始材料接入能力

支持三类输入源，可组合使用：

- **音频文件上传**：支持 mp3/wav/m4a，通过本地离线 ASR 完成语音转写，不调用第三方语音 API；
- **ASR 引擎双模可选**：
  - **主选：Faster-Whisper**（本地离线，CPU/GPU 均可运行，INT8 量化优化，无需联网）
  - **备选：Qwen3-ASR-Flash**（通义千问云端 ASR API，通过阿里百炼 DashScope 接入，中文场景准确率更高，需网络和 API Key）
- **文本资料上传**：支持 SRT 字幕文件，直接解析得到会议对话文本，跳过 ASR 环节，适合快速迭代调试 Prompt；
- **参考文档上传**：支持 docx / pdf / txt / md 格式，作为评审需求基线，解析文本后与语雀拉取文档统一纳入上下文。

**优势**：不需要会议厂商企业版账号，普通录音文件即可使用；无语雀权限时通过本地文档上传获得同等基线能力。

#### 3.1.1 ASR 引擎方案对比

| 对比维度 | Faster-Whisper（主选） | Qwen3-ASR-Flash（备选） |
|----------|----------------------|------------------------|
| **部署方式** | 本地离线部署，模型下载至本地 | 云端 API 调用（阿里百炼 DashScope） |
| **硬件要求** | CPU 可运行（INT8 优化），有 GPU 效果更佳 | 无需本地 GPU，云端处理 |
| **网络要求** | 离线运行，无需联网 | 需联网调用 API |
| **数据安全** | ✅ 音频不出内网 | ⚠️ 音频需上传阿里云端 |
| **成本** | 免费开源 | 按 API 调用量计费 |
| **中文准确率** | 良好（small 模型可满足多数场景） | 优秀（阿里云训练，中文场景通常更优） |
| **延迟** | 取决于本地 CPU/GPU 性能 | 取决于网络 + 云端处理速度 |
| **维护成本** | 需管理模型文件（~500MB+） | 零维护，接入 API Key 即可 |
| **适用场景** | 普通会议、敏感数据会议（内网闭环） | 对准确率要求高的会议、有 GPU 算力瓶颈时 |

**选择建议**：
- **默认使用 Faster-Whisper**，满足数据内网闭环要求，零成本
- 当 Faster-Whisper 识别准确率不足（如口音重、专业术语多），可切换至 Qwen3-ASR-Flash
- 敏感会议强制使用 Faster-Whisper，确保音频不出内网

### 3.2 参考文档接入与会议管理（语雀 + 本地文档 + 拉取记录）

支持两种参考文档来源，并通过会议管理串联业务流程：

#### 3.2.1 语雀 PRD 文档拉取

- 用户在界面配置多来源语雀知识库（不同 Token 对应不同知识库），支持 CRUD 管理；
- **需求号拉取**：用户选择来源 + 输入需求号（如 SCPRO-1071），后端在知识库 TOC 中搜索匹配的 TITLE 节点，递归拉取该节点下所有 DOC 文档；
- 拉取的文档自动保存为快照（Snapshot），同时生成**拉取记录**，支持查看历史、重新拉取、删除；
- 拉取记录详情页采用**左右分栏**布局：左侧文件树展示文档列表，右侧语雀风格渲染文档内容；
- 图片处理：语雀文档中的图片托管在 `cdn.nlark.com`，需带 Cookie 认证。后端提供 `/api/yuque-image-proxy` 代理接口，自动附加 `_yuque_session` / `yuque_ctoken` 凭据转发，前端渲染时自动替换图片链接；
- **快照机制**：拉取到的文档内容存入数据库和本地文件系统作为本次会议快照；后续线上语雀文档修改，不影响历史会议回看；

#### 3.2.2 本地参考文档上传

- 支持上传 docx / pdf / txt / md 格式文件，作为评审需求基线；
- 后端自动解析提取文本内容，生成与语雀拉取统一的参考文档快照；

#### 3.2.3 会议管理（串联参考文档与音频转写）

- **会议/需求记录管理**：用户创建会议记录，填写业务标题、业务背景，关联参考文档；
- **三种资源关联方式**：
  1. **本地上传**：直接上传本地文件，自动加入当前会议；
  2. **语雀拉取**：选来源 + 输入需求号实时拉取，结果自动加入会议；
  3. **拉取记录**：从历史拉取记录中树形选择，支持勾选/全选/半选子文件；
- **音频上传关联**：上传音频时选择关联会议，自动带入业务背景和参考文档；
- **工作台关联展示**：任务列表显示关联会议，支持点击更换或取消关联；
- **任务详情**：显示关联会议名称；

#### 3.2.4 统一约束

- 参考文档（无论来源）仅作为业务基线，严禁 AI 编造文档中未讨论的内容为评审结论；
- 所有参考文档快照均持久化存储，支持历史回溯。

### 3.3 AI 纪要生成引擎（提示词模板 + 自定义提示词）

三层 AI 控制逻辑，适配不同会议类型与高级用户调试：

- **内置系统模板库**：需求评审模板、技术评审模板、周会例会模板；
- **业务背景输入**：用户填写本次会议项目版本、参会人员、历史遗留问题等上下文；
- **自定义提示词**：高级用户可完全覆盖系统模板，自定义 AI 输出规则、格式、约束，方便切换调试不同大模型。

#### 3.3.1 LLM 适配器（模型无关架构）

为实现模型无关性，定义统一 LLM 调用接口：

- **LLMAdapter 抽象层**：定义 `chat(messages, temperature, max_tokens)` 统一接口，屏蔽不同模型 API 差异；
- **OpenAI 兼容模式**（优先实现）：DeepSeek、通义千问、GLM 等均提供 OpenAI 兼容 API，通过配置 `base_url` 和 `api_key` 即可切换，无需额外适配；
- **原生 SDK 扩展**（后续）：需要时再实现 DashScope、Zhipu 等原生 SDK 适配；
- **提示词兼容性**：切换模型后需重新验证提示词效果，不同模型对系统提示词的敏感度不同。

#### 3.3.2 输入组装规则

**输入组装规则**（user 消息由中台自动拼接）：

```
【业务背景】
{用户填写背景}

【原始需求参考文档】
{语雀快照 / 上传文档快照 / 手动粘贴参考文本，无参考文档填"无参考文档"}

【会议转写文本】
{ASR/SRT 解析得到的会议对话}
```

#### 3.3.3 输出策略

**输出策略**（二阶段，提高稳定性）：

一阶段：模型输出 Markdown 格式纪要（适合阅读）；
二阶段：基于 Markdown 结果，调用结构化输出（`response_format` / `json_schema` 参数）生成 JSON（确保格式合法）；

若模型支持 `response_format` 参数（如 DeepSeek、通义千问），优先使用该方式直接输出合法 JSON；
若模型不支持结构化输出，则采用正则提取 JSON 代码块 + `json5` 宽松解析 + 失败时 LLM 自修复的三级降级方案。

**内置防幻觉约束**：所有决策、变更、待办必须来源于会议转写文本；参考文档仅作为基线，未在会议讨论的条目，禁止输出评审结论。

### 3.4 多渠道消息通知适配

当前实现钉钉机器人推送：

- 界面输入钉钉 Webhook 地址与安全密钥，为选填项，不填则不推送；
- 内部完成 Markdown 语法适配，兼容钉钉机器人消息格式；
- API 层面支持传入钉钉 Webhook 与密钥参数；

**限制**：
- 原型阶段仅实现消息推送；暂不实现自动创建钉钉待办任务；
- **钉钉推送需要部署机器有公网访问能力**（或配置 HTTP 代理访问公网），纯内网环境无法使用此功能；
- 推送内容仅包含纪要摘要，不包含原始音频和完整转写文本。

### 3.5 数据归档与历史资产管理

数据库保存完整会议记录，可在 Web 界面查看历史列表：

- 会议主题、会议类型、业务背景；
- 当时生效的系统 / 自定义提示词；
- 语雀 PRD 快照 / 上传文档快照内容；
- 原始会议转写文本；
- Markdown 纪要、结构化 JSON 结果；
- 推送渠道信息、创建时间；
- **拉取记录**：每次语雀需求拉取的历史记录，支持重新拉取；
- **任务关联会议**：转写任务可关联会议，支持更换或取消关联。

### 3.6 异步任务队列

所有耗时操作（ASR 转写、大模型调用）均通过异步任务队列处理，避免 HTTP 超时和 CPU 占满。

**设计要点**：
- **任务提交**：客户端提交请求，服务端立即返回 `task_id`，任务进入后台队列；
- **任务状态**：`pending` → `processing` → `completed` / `failed`，状态持久化到数据库；
- **结果获取**：客户端通过 `GET /api/v1/tasks/{task_id}` 轮询获取结果，后续可扩展 WebSocket 实时推送；
- **队列实现**：原型阶段使用内存队列 + 数据库状态表，无需引入 Celery/RabbitMQ；
- **并发控制**：单机模式下限制同时处理 1 个任务，后续任务排队等待；
- **超时处理**：单个任务超过 30 分钟标记为超时失败，返回错误提示。

**短会议阈值**：音频时长 ≤ 30 分钟可同步处理（仍建议走异步）；超过 30 分钟必须异步。

### 3.7 标准化对外 API 能力

对外提供 HTTP 接口，支持外部程序调用核心纪要能力。

**主要入参**：会议主题、会议类型、业务背景、自定义系统提示词、音频 / SRT 文件、参考文档（yuque_doc_url / 上传文档文件）、钉钉 Webhook 及密钥。

**返回**：Markdown 文本、结构化 JSON、任务编号。

---

## 4 完整业务处理流程

1. 用户 Web 端录入：会议主题、业务背景，选择会议模板或填写自定义提示词；
2. 选择材料来源：上传音频 / 上传 SRT 字幕 / 上传参考文档 / 填写语雀文档链接；
3. 中台处理原始材料：
   - 音频文件：执行本地 ASR 语音转写（默认 Faster-Whisper，可选 Qwen3-ASR-Flash）；
   - SRT 文件：解析得到会议文本；
   - 语雀链接：后端拉取 PRD 文档并生成快照入库；
   - 本地文档：解析 docx/pdf/txt/md 提取文本并生成快照入库；
4. 中台组装完整 Prompt 上下文，调用大模型服务；
5. 得到 Markdown + JSON 双格式纪要结果；
6. 如果配置钉钉 Webhook，则执行钉钉消息推送；
7. 全部元数据与结果写入数据库归档；
8. 用户可在页面预览结果，查看历史会议记录。

---

## 5 内部模块拆解

| 模块 | 职责 |
|------|------|
| 参考文档适配器模块（语雀 + 本地文档） | 语雀 OpenAPI 调用（HTML→Markdown 转换）、本地文档解析(docx/pdf/txt/md，含 PDF 扫描件检测)、文档快照存储（含图片资源） |
| 本地离线 ASR 语音转写模块 | Faster-Whisper 音频转写（主选），Qwen3-ASR-Flash 云端 ASR（备选），支持双模切换 |
| LLM 适配器模块 | 统一大模型调用接口（`chat(messages, temperature, max_tokens)`），支持 OpenAI 兼容模式切换 DeepSeek/通义千问/GLM |
| AI 纪要推理引擎 | 模板管理、上下文组装、二阶段输出策略（Markdown + JSON） |
| 异步任务队列模块 | 任务提交与状态管理（`pending→processing→completed/failed`）、轮询/回调结果获取、并发控制 |
| 钉钉消息推送适配模块 | 钉钉 Webhook Markdown 消息推送（需公网访问） |
| Web 可视化 UI 模块 | 表单录入、文件上传、结果预览、历史查看 |
| HTTP API 接入网关模块 | 标准化对外接口、健康检查、任务状态查询 |
| 数据持久化存储模块 | 数据库操作（SQLite WAL 模式）、文件存储管理、归档与全文检索 |

---

## 6 质量与防幻觉设计

### 6.1 AI 幻觉约束策略

在系统提示词内置强制约束规则：

1. 全部决策、变更、待办、风险，必须来自会议转写文本；
2. PRD 参考文档仅作为业务基线，文档有但会上未讨论的内容，严禁编造评审结论；
3. 会议口头讨论优先级高于 PRD 文档，发生冲突时以会议为准并标注变更；
4. 识别不到责任人、时间，统一标记【未指定责任人】【时间待确认】，禁止 AI 自行编造；
5. 过滤闲聊、跑题内容，只保留有效业务信息。

### 6.2 上下文超限防护策略

- 中台层对参考文档（语雀 / 本地上传）、会议转写做长度监控，基于 token 数精确计算（使用 `tiktoken` 或各模型的 tokenizer）；
- **三级截断策略**：
  1. **轻度超限（<10%）**：按比例均匀压缩会议转写文本（保留关键时间戳段落）；
  2. **中度超限（10-50%）**：完整保留会议转写，截断 PRD 参考文档（保留首尾章节 + 关键标题）；
  3. **严重超限（>50%）**：返回错误提示，建议用户缩短输入内容；
- 不同大模型上下文窗口不同（DeepSeek 128K, GLM 128K, 通义千问 100K+），截断策略随模型配置动态调整；
- 在前端显示当前上下文占用情况（已使用 / 总上限）；

---

## 7 部署方案

### 7.1 部署形态

单机轻量化部署，运行在内网机器，不需要 K8s、微服务集群。

- 原型阶段：开发人员笔记本直接运行；
- 部门试用阶段：内网单机服务器部署；
- 后期可平滑升级：SQLite 迁移 MySQL，增加异步任务队列，改造为正式中台。

**数据库配置**：
- 原型阶段启用 SQLite WAL 模式（Write-Ahead Logging），提升并发读写性能；
- 配置 `check_same_thread=False`，支持 FastAPI 异步框架的多线程访问；
- 大字段（转写文本、文档内容、纪要结果）采用文件存储 + 数据库记录路径的方式，避免 SQLite 单行大字段性能瓶颈。

**服务健康检查**：
- 提供 `/health` 端点，返回服务状态（数据库连接、ASR 模型加载状态、磁盘空间）；
- 日志记录采用结构化格式（JSON），包含请求 ID、操作类型、耗时等关键字段。

**文件存储目录结构**：
```
data/
  uploads/          ← 上传的原始文件
    audio/          ← 音频文件（处理后定期清理）
    docs/           ← 参考文档
  snapshots/        ← 快照文件
    yuque/          ← 语雀文档快照（含图片资源）
    local/          ← 本地文档快照
  outputs/          ← 生成结果
    markdown/
    json/
  temp/             ← 临时文件（定期清理）
```

### 7.2 软硬件环境要求

| 项目 | 说明 |
|------|------|
| 软件环境 | Python 3.10+；如需运行本地 ASR，Faster-Whisper 依赖 PyAV（自带 FFmpeg 库，无需系统级安装 ffmpeg） |
| 硬件（CPU 模式） | 可运行音频转写，推荐 INT8 量化（`compute_type="int8"`），长音频速度较慢，建议使用 small 模型 |
| 硬件（N 卡 GPU） | 大幅提升 Faster-Whisper 音频转写速度，支持 FP16 推理 |
| 依赖项 | 大模型 API Key；语雀只读 Token（可选）；Python 文档解析库（python-docx、PyMuPDF 等）；不需要公网暴露服务 |
| 文件上传安全 | 限制音频文件 ≤ 200MB，文档 ≤ 20MB；MIME type + magic bytes 双重校验；采用随机文件名存储，避免路径遍历 |

---

## 8 实施周期与里程碑

| 里程碑 | 内容 | 预估工时（工作日） | 状态 |
|--------|------|-------------------|------|
| M1 基础能力搭建 | 项目初始化、ASR 模块集成、异步任务队列、音频上传、前端骨架 | 4-6 天 | ✅ 完成 (2026-08-17) |
| M2 参考文档+会议管理 | 文档解析、语雀拉取、来源管理、拉取记录、会议管理、关联任务、图片代理 | 6-8 天 | ✅ 完成 (2026-08-17) |
| M3 AI 纪要生成 | LLM 适配器、模板管理、上下文组装、大模型调用、Markdown + JSON 双格式输出 | 4-6 天 | ⬜ 待开始 |
| M4 输出与存储 | 钉钉推送适配、数据持久化、历史查询 API | 3-4 天 | ⬜ 待开始 |
| M5 前端界面 | Web 界面完善（交互优化、错误处理、响应式） | 2-3 天 | ⬜ 待开始 |

**当前进度**：M1-M2 已完成，M3 待开始。核心后端能力 + 前端管理界面已就绪，共 19-29 工作日。

**注意**：若优先交付 API + CLI 模式（跳过 M5 前端），核心能力可在 2-3 周内完成，供开发和测试人员先行试用。

---

## 9 风险点与应对措施

| 风险 | 应对措施 |
|------|----------|
| 大模型输出不稳定，JSON 格式偶现异常 | 采用二阶段输出策略（先 Markdown 后 JSON）；优先使用结构化输出参数；支持宽松解析 + LLM 自修复三级降级 |
| ASR 识别存在错字 | 保留 SRT 文本输入模式，重要会议优先使用校对后的 SRT；Faster-Whisper 准确率不足时可切换 Qwen3-ASR-Flash 云端 ASR 作为备选 |
| 语雀 Token 权限变更、文档被移动导致拉取失败 | 界面友好报错；支持本地文档上传作为降级方案；快照已保存历史版本 |
| 长音频转写超时阻塞服务 | 实现异步任务队列，提交 → 返回 task_id → 轮询结果；音频超过 30 分钟强制异步 |
| 大模型 API 调用失败（网络超时/限流/预算耗尽） | 指数退避重试（最多 3 次）；配置合理超时参数（timeout=120s）；记录每次调用 token 消耗，支持熔断机制 |
| 扫描件 PDF 无法提取文本 | 上传时检测 PDF 类型，扫描件提示用户改用原始文档（docx/txt/md） |
| 文件上传安全风险（过大/恶意文件） | 限制文件大小（音频 ≤ 200MB，文档 ≤ 20MB）；MIME + magic bytes 双重校验；随机文件名存储 |

---

## 10 后续演进规划

当前为 V1.0 原型版本，后续可按需迭代增强：

- [ ] 纪要待办自动生成钉钉待办任务；
- [ ] 评审结果回写语雀文档；
- [ ] 对接企业会议开放平台，实现会议结束自动拉取转写；
- [ ] 增加简单用户权限控制；
- [ ] 构建会议决策 RAG 知识库，支持历史会议检索问答；
- [ ] 增加 PaddleOCR 或 Tesseract 支持扫描件 PDF 文本提取；
- [ ] 支持 WebSocket 实时推送任务进度。

---

## 11 方案总结

本方案构建轻量化 AI 会议纪要中台，解决评审会议纪要痛点。核心差异化能力是语雀 PRD 基线对照评审，弥补普通纪要工具只有录音文本、缺少需求上下文的短板。

整体架构模型无关，可灵活切换大模型；不依赖会议厂商企业版权限，前期原型落地成本低；同时中台化架构预留扩展，未来可以演进为企业级正式服务。