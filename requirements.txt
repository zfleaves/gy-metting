# AI 智能会议纪要轻量化中台
# 基于 DESIGN.md V1.1
# 安装: pip install -e .[dev]
# 运行: python main.py

# ---- 核心依赖 ----

# Web 框架
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# 配置管理
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 数据库
sqlalchemy>=2.0.0

# ASR 语音转写（本地离线）
faster-whisper>=1.0.0

# 文档解析
python-docx>=0.8.0
PyMuPDF>=1.23.0

# Token 计数（上下文超限防护）
tiktoken>=0.5.0

# HTTP 客户端（语雀 API、钉钉 Webhook）
requests>=2.25.0
httpx>=0.24.0

# 文件上传
python-multipart>=0.0.6
aiofiles>=23.0.0

# 配置解析
PyYAML>=5.4

# ---- 开发依赖 ----
# 安装: pip install -r requirements-dev.txt