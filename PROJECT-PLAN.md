# 项目计划表

> 基于 [DESIGN.md §8 实施周期与里程碑](DESIGN.md) 制定
> 最后更新：2026-08-17
> 策略：**前后端并行**，每个阶段都有可交互页面，及时验证、及时调整

## 总览

| 阶段 | 后端内容 | 前端内容 | 预估工时 | 状态 |
|------|---------|---------|---------|------|
| [准备阶段](#准备阶段) | 工程骨架 | — | — | ✅ 完成 |
| [M1 基础能力](#m1-基础能力) | FastAPI + ASR + 任务队列 | 前端骨架 + 音频上传页 | 4-6 天 | ✅ 完成 (2026-08-17) |
| [M2 参考文档+会议管理](#m2-参考文档会议管理) | 文档解析 + 语雀拉取 + 会议管理 + 拉取记录 | 完整管理页面体系 | 6-8 天 | ✅ 完成 (2026-08-17) |
| [M3 AI 纪要](#m3-ai-纪要) | LLM 适配器 + 纪要生成 | 纪要结果展示页 | 4-6 天 | ⬜ 待开始 |
| [M4 输出与存储](#m4-输出与存储) | 钉钉推送 + 存储 + 查询 | 历史列表 + 搜索 | 3-4 天 | ⬜ 待开始 |
| [M5 收尾打磨](#m5-收尾打磨) | 健康检查 + 日志完善 | 交互优化 + 错误处理 | 2-3 天 | ⬜ 待开始 |

**总计：19-29 工作日（约 4-6 周）**，每个阶段结束都能看到可交互的页面。

---

## 准备阶段 ✅

> 2026-08-10 完成

- [x] 初始化 Python 项目骨架（`pyproject.toml`、`src/`、`main.py`）
- [x] 创建主 `requirements.txt`（FastAPI、faster-whisper、SQLAlchemy 等）
- [x] 创建 `data/` 目录结构（按 DESIGN.md §7.1）
- [x] 完善 `README.md`（项目介绍、快速开始、架构图引用）
- [x] 创建 `tests/` 目录占位
- [x] 语雀拉取脚本（`scripts/yuque_pull.py`，从 chongya-miniapp 复用）

---

## M1 基础能力

> 预估 4-6 天。**目标：页面能上传音频，看到 ASR 转写结果。**

### 后端

- [x] 配置管理模块（从 `.env` 加载，pydantic-settings）
- [x] FastAPI 应用框架（`app.py`、路由注册、CORS、异常处理、前端 SPA 托管）
- [x] 日志模块（结构化 JSON 日志，请求 ID 追踪）
- [x] 数据库初始化（SQLite WAL，SQLAlchemy 模型，迁移脚本）
- [x] ASR 引擎抽象层（`src/asr/base.py`，统一接口 + 进度回调）
- [x] ASR 模块 — Faster-Whisper 集成（`src/asr/whisper_engine.py`，线程池）
- [x] 异步任务队列（`src/task/`，内存队列 + 状态机 + 数据库状态表）
- [x] 音频上传 API（`POST /api/upload/audio`，含去重）
- [x] 任务提交/查询 API（`POST /api/tasks`、`GET /api/tasks/{id}`、`DELETE`）
- [x] 任务名称 + 进度细粒度回调 + 删除功能

### 前端（同步）

- [x] 前端技术选型：**Vue 3**（Composition API + Vite + Vue Router）
- [x] 前端项目骨架（`frontend/`，Vite 初始化、路由、布局、导航、主题）
- [x] 音频上传页面（拖拽/选择文件、进度条、格式校验）
- [x] 转写结果展示页（实时轮询任务状态、文本渲染、分段对照、重点标记）
- [x] 前端与后端 API 联通
- [x] 登录/注册 + 用户管理 + 中台管理布局 + 响应式 + 任务删除

### 验收标准

> 打开浏览器 → 上传音频文件 → 等待转写 → 看到转写文本

---

## M2 参考文档+会议管理

> 预估 6-8 天。**目标：建立完整的会议/需求上下文体系，串联音频上传与参考文档。**

### 后端

- [x] 文档解析统一接口（`src/doc/parser.py`，`parse(file) → str`）
- [x] 本地文档解析器 — docx（`python-docx`）
- [x] 本地文档解析器 — pdf（`PyMuPDF`，含扫描件检测）
- [x] 本地文档解析器 — txt/md（直接读取）
- [x] 语雀拉取集成（`src/doc/yuque.py`，调用 `scripts/yuque_pull.py`）
- [x] 文档快照存储（`src/doc/snapshot.py`，DESIGN.md §3.2.1）
- [x] 文档上传 API（`POST /api/documents/upload`）
- [x] 语雀拉取 API（`POST /api/documents/yuque`）
- [x] 文档快照查询 API（`GET /api/documents`、`GET /api/documents/{id}`）
- [x] 语雀来源管理（`src/api/yuque_source.py`，用户级多来源 CRUD）
- [x] 需求号拉取（`POST /api/yuque-sources/{id}/pull`，TOC 搜索 + 递归拉取 + 快照入库）
- [x] 会议 API（`src/api/meetings.py`，CRUD + 关联文档快照）
- [x] 拉取记录 API（`src/api/yuque_records.py`，列表 + 详情 + 删除 + 重新拉取）
- [x] 会议关联任务 API（`PATCH /api/tasks/{id}/meeting`，支持更换）
- [x] 语雀图片代理 API（`src/api/yuque_image_proxy.py`，带 Cookie 认证转发 CDN 图片）

### 前端（同步）

- [x] 文档上传组件（拖拽上传，支持 docx/pdf/txt/md）
- [x] 语雀来源管理页面（表格管理 + 弹窗新增/编辑，敏感字段编辑时 disabled 展示）
- [x] 需求号拉取页面（选择来源 → 输入需求号 → 拉取所有关联文档）
- [x] 拉取结果展示（文档列表 + 状态标识 + 点击预览）
- [x] 拉取记录列表页（表格管理 + 搜索 + 详情/重新拉取/删除）
- [x] 拉取记录详情页（左右分栏：左侧文件树 + 右侧语雀风格文档预览，marked 渲染）
- [x] 会议管理页（表格列表 + 搜索 + 创建/编辑弹窗）
- [x] 会议创建弹窗（标题 + 业务背景 + 三种资源关联方式）
  - [x] 本地上传（即时上传并加入会议）
  - [x] 语雀拉取（实时拉取，结果自动加入会议）
  - [x] 拉取记录（树形展开，勾选/全选/半选子文件）
- [x] 会议详情页（业务背景展示 + 左右分栏文件预览）
- [x] 工作台任务关联会议（展示 + 点击更换/取消关联弹窗）
- [x] 上传页关联会议选择（下拉框 + 上传后关联标签展示）
- [x] 任务详情页关联会议展示
- [x] 文档预览（图片代理，语雀 CDN 图片正常显示）

### 验收标准

> 参考文档页 → 创建会议、填写背景、关联语雀文档/上传文件 → 上传音频时选择关联会议 → 工作台显示关联 → 文档详情页左右分栏预览

---

## M3 AI 纪要

> 预估 4-6 天。**目标：填完表单 → 等一会 → 看到 AI 生成的纪要。**

### 后端

- [ ] LLM 适配器抽象层（`src/llm/adapter.py`，`chat(messages, ...)` 统一接口）
- [ ] OpenAI 兼容模式实现（`src/llm/openai_compat.py`）
- [ ] 提示词模板管理（`src/llm/templates/`，需求评审/技术评审/周会）
- [ ] 业务背景输入 + 自定义提示词支持
- [ ] 上下文组装引擎（`src/llm/context.py`，DESIGN.md §3.3.2）
- [ ] 三级截断策略（DESIGN.md §6.2，基于 tiktoken）
- [ ] 二阶段输出策略（先 Markdown 后 JSON，DESIGN.md §3.3.3）
- [ ] 防幻觉约束注入（DESIGN.md §6.1，5 条强制规则）
- [ ] LLM 重试与熔断（指数退避，最多 3 次，DESIGN.md §9）
- [ ] 纪要生成 API（`POST /api/minutes/generate`）

### 前端（同步）

- [ ] 会议表单页（主题、会议类型、业务背景、自定义提示词）
- [ ] 模板选择器（需求评审/技术评审/周会，切换预览）
- [ ] 纪要结果展示页（Markdown 渲染 + JSON 切换）
- [ ] 上下文占用指示器（已使用/总上限，DESIGN.md §6.2）
- [ ] 任务进度实时展示（轮询/SSE）

### 验收标准

> 选模板 → 填背景 → 上传音频+文档 → 点击生成 → 等待 → 看到 Markdown 纪要 + JSON 结构化数据

---

## M4 输出与存储

> 预估 3-4 天。**目标：纪要可推送钉钉，历史记录可翻查。**

### 后端

- [ ] 输出适配器抽象层（`src/output/base.py`，预留多渠道）
- [ ] 钉钉 Webhook 推送（`src/output/dingtalk.py`，Markdown 消息）
- [ ] 数据持久化模块（`src/storage/db.py`，SQLAlchemy ORM）
- [ ] 会议元数据模型（Task、Meeting、Snapshot、Output）
- [ ] 历史查询 API（`GET /api/meetings`、`GET /api/meetings/{id}`，分页+搜索）
- [ ] 大字段文件存储（DB 存路径，文件存 `data/`）

### 前端（同步）

- [ ] 历史会议列表页（分页、搜索、筛选）
- [ ] 会议详情页（纪要内容、原始文档、转写文本��看）
- [ ] 钉钉推送状态展示（成功/失败/未推送）

### 验收标准

> 历史列表可翻查 → 点击进入详情 → 可回看纪要、原始转写、参考文档 → 可手动推送钉钉

---

## M5 收尾打磨

> 预估 2-3 天。**目标：完整可用，交互流畅，错误处理到位。**

### 后端

- [ ] 健康检查端点（`/health`，DB 连接、ASR 状态、磁盘空间）
- [x] 日志完善（全链路请求 ID、耗时统计、JSON 格式）
- [ ] 错误处理与友好提示
- [ ] 集成测试（端到端流程）

### 前端

- [ ] 全局错误处理（网络异常、超时、服务不可用）
- [ ] 加载状态与骨架屏
- [ ] 响应式适配（移动端可用）
- [ ] 交互细节优化（过渡动画、提示文案）

### 验收标准

> 完整流程走通无卡顿 → 异常场景有友好提示 → 移动端基本可用

---

## 后续演进（V2+）

> 见 DESIGN.md §10，原型阶段不实现。

- [ ] 纪要待办自动生成钉钉待办任务
- [ ] 评审结果回写语雀文档
- [ ] 对接企业会议开放平台
- [ ] 用户权限控制
- [ ] 会议决策 RAG 知识库
- [ ] 扫描件 PDF OCR 支持
- [ ] WebSocket 实时推送任务进度